Look through my code and list small methods (say less then 20 lines) that are not tested under pytest. Then for each of them suggest test(s). Put your results in the html file claude/test_review.html, under a dark theme.

Ignore the content of sdevpy/thirdparty as it is third-party and I have no intent of touching the code for now.

Put your results in the html file claude/output/test_review.html. Use the following file as example: claude/formats/test_review.example.html.

Before reporting any issue, verify it exists in the current working copy by reading the relevant file directly with the Read tool at the exact line numbers. Do not report issues based solely on subagent summaries. Discard any finding that cannot be confirmed in the actual file. Do not use cached summaries coming from previous runs of the task.

